edition = "2024"
hard_tabs = false
tab_spaces = 4
max_width = 120
use_small_heuristics = "Max"
# `imports_granularity` (formerly `merge_imports`) is still unstable and only available in nightly.
# imports_granularity = "Crate"